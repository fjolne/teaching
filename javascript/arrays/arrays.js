/**
 * Calculate n! which is called a factorial of number n (n >= 1).
 * Factorial is a product of numbers from 1 to n, i.e. n! = 1 * 2 * 3 * ... * (n-1) * n
 *
 * Hint: you don't need arrays for this one and you can use any of the loop statements.
 *
 * @param {number} n
 * @returns {number} the factorial
 */
export function factorial(n) {
  throw new Error("Remove this line and implement the function");
}

/**
 * Given array a and number x, return a new array which consists of elements of a
 * which are greater than x.
 *
 * Hint: you can create a new array b and use b.push(...) to add elements to it.
 *
 * @param {number[]} a
 * @param {number} x
 * @returns {number[]} the number of minutes remaining
 */
export function filterGreater(a, x) {
  throw new Error("Remove this line and implement the function");
}
