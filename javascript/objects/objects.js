/**
 * You are given an object with number values. Find the sum of all values.
 * valuesSum({a: 10, b: 12}) == 24
 *
 * @param {Object<string, number>} obj
 * @returns {string}
 */
export function valuesSum(obj) {
  throw new Error("Remove this line and implement the function");
}

/**
 * You are given an object with number values. Return the new object with odd-valued entries from the input.
 * filterOdd({a: 10, b: 11, c: 12}) == {b: 11}
 *
 * @param {Object<string, number>} obj
 * @returns {Object<string, number>}
 */
export function filterOdd(obj) {
  throw new Error("Remove this line and implement the function");
}

/**
 * You are given an object with string keys and values. Transform it into a string representation,
 * following the rules shown in the examples below. You may place key-value pairs in any order.
 * formatObject({}) == ""
 * formatObject({a: "b"}) == "a=b"
 * formatObject({a: "b", c: "d"}) == "a=b,c=d" or "c=d,a=b"
 *
 * @param {Object<string, string>} obj
 * @returns {string}
 */
export function formatObject(obj) {
  throw new Error("Remove this line and implement the function");
}
