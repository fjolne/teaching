import { implementMe } from "./template";

describe("TEMPLATE", () => {
  beforeEach(() => {
    // setup
  });

  describe("group name 1", () => {
    test("test name 1", () => {
      expect(implementMe()).toEqual([1, 2, 3, 4, 5]);
    });
  });
});
